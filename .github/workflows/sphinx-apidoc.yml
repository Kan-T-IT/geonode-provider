#name: Publish Github Pages Sphinx

#on:
  #push:
    #branches: [sphinx-actions]
#jobs:
  #deploy:
    #runs-on: ubuntu-latest
    
    #steps:
    #- name: Set up Python
     # uses: actions/setup-python@v3
    #  with:
   #     python-version: '3.x'
   # - name: Install dependencies
   #   run: |
   #     python -m pip install --upgrade pip
   #     pip install -U sphinx sphinx-intl sphinx-rtd-theme sphinxcontrib-openapi
   #     sphinx-apidoc -o docs geonode_provider

name: Build Docs

on:
  push:
    branches:
      - sphinx-actions
  pull_request:
    branches:
      - sphinx-actions

jobs:
  build-docs:
    runs-on: ubuntu-latest

    steps:
    # 1. Checkout your repository
    - name: Checkout repository
      uses: actions/checkout@v3

    # 2. Set up Python
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'

    # 3. Install dependencies
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install sphinx sphinx-intl sphinx-rtd-theme sphinxcontrib-openapi

    # 4. Run sphinx-apidoc
    - name: Generate API documentation
      run: |
        sphinx-apidoc -o docs geonode_provider

    # 5. Build the HTML docs (opcional)
    - name: Build HTML documentation
      run: |
        sphinx-build -b html docs docs/_build/html

